apiVersion: v1
kind: Service
metadata:
  name: grafana-service
spec:
  type: NodePort
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 3000        # port interne du service
      targetPort: 3000  # port du container
      nodePort: 32000   # port externe pour accéder depuis ton PC
